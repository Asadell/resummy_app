import 'package:resummy_app/features/cv_tools/domain/repositories/cv_repository.dart';

class CvRepositoryImpl implements CvRepository {
  // This class was used for Clean Architecture implementation
  // Now using direct service pattern for CV Analyzer.
  // Keeping this empty or removing analyzeCv as it's no longer used via this repository.
}
